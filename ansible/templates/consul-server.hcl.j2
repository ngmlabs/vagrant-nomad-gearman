datacenter = "test"

data_dir = "/opt/consul"

client_addr = "0.0.0.0"
advertise_addr = "{{ ansible_eth1.ipv4.address }}"

ui = true

server = true

bootstrap_expect=1

retry_join = ["{{ groups['servers'] | map('extract', hostvars, ['ansible_eth1', 'ipv4', 'address']) | join('", "') }}"]
